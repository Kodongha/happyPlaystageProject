selectOne=SELECT * FROM USER_INFO  WHERE USER_SEQ = ?
insertLevelOne=INSERT INTO USER_GRADE_UP VALUES(USER_GRADE_UP_SEQ.NEXTVAL, ?, '\uC2E0\uCCAD', SYSDATE, DEFAULT)
selectListCount=SELECT COUNT(*) FROM RENT_PROP WHERE USER_SEQ=? 
searchCheck=SELECT * FROM RENT_PROP R JOIN USER_INFO U ON( U.USER_SEQ = R.USER_SEQ) WHERE RENT_SEQ=?
selectListWithPaging=SELECT RNUM, HALL_NM, PROP_SEQ, RENT_SEQ, USER_SEQ, PROP_NM, PROP_DT, PROP_STATUS, USE_START_DT, USE_END_DT FROM ( SELECT ROWNUM RNUM, B.HALL_NM, P.PROP_SEQ, P.RENT_SEQ, P.USER_SEQ, P.PROP_NM, P.PROP_DT, P.PROP_STATUS, P.USE_START_DT, P.USE_END_DT FROM RENT_PROP P JOIN RENT_BASIC B ON ( P.RENT_SEQ = B.RENT_SEQ) JOIN USER_INFO I ON( P.USER_SEQ = I.USER_SEQ) WHERE P.USER_SEQ = ?) WHERE RNUM BETWEEN ? AND ?
selectListCount1=SELECT COUNT(*) FROM RENT_PROP WHERE USE_START_DT IS NOT NULL AND USE_END_DT IS NOT NULL AND USER_SEQ=? 
selectListWithPaging1=SELECT RNUM, HALL_NM, COMP_NM, PROP_SEQ, RENT_SEQ, USER_SEQ, PROP_NM, PROP_DT, PROP_STATUS, USE_START_DT, USE_END_DT FROM ( SELECT ROWNUM RNUM, B.HALL_NM, B.COMP_NM, P.PROP_SEQ, P.RENT_SEQ, P.USER_SEQ, P.PROP_NM, P.PROP_DT, P.PROP_STATUS, P.USE_START_DT, P.USE_END_DT FROM RENT_PROP P JOIN RENT_BASIC B ON ( P.RENT_SEQ = B.RENT_SEQ) JOIN USER_INFO I ON( P.USER_SEQ = I.USER_SEQ) WHERE P.USER_SEQ = ? AND PROP_STATUS = '\uC644\uB8CC') WHERE RNUM BETWEEN ? AND ?
selectListCount2=SELECT COUNT(*) FROM RENT_PROP WHERE USE_START_DT IS NOT NULL AND USE_END_DT IS NOT NULL AND RENT_SEQ=? AND USER_SEQ=?
searchCheck=SELECT RNUM, HALL_NM, PROP_SEQ, RENT_SEQ, USER_SEQ, PROP_NM, PROP_DT, PROP_STATUS, USE_START_DT, USE_END_DT FROM ( SELECT ROWNUM RNUM, B.HALL_NM, P.PROP_SEQ, P.RENT_SEQ, P.USER_SEQ, P.PROP_NM, P.PROP_DT, P.PROP_STATUS, P.USE_START_DT, P.USE_END_DT FROM RENT_PROP P JOIN RENT_BASIC B ON ( P.RENT_SEQ = B.RENT_SEQ) JOIN USER_INFO I ON( P.USER_SEQ = I.USER_SEQ) WHERE P.USER_SEQ = ? AND P.RENT_SEQ = ?) WHERE RNUM BETWEEN ? AND ?
selectListCount3=SELECT COUNT(*) FROM RENT_PROP WHERE USE_START_DT IS NOT NULL AND USE_END_DT IS NOT NULL AND RENT_SEQ=? AND USER_SEQ=?
searchCheck1=SELECT RNUM, HALL_NM, COMP_NM, PROP_SEQ, RENT_SEQ, USER_SEQ, PROP_NM, PROP_DT, PROP_STATUS, USE_START_DT, USE_END_DT FROM ( SELECT ROWNUM RNUM, B.HALL_NM, B.COMP_NM, P.PROP_SEQ, P.RENT_SEQ, P.USER_SEQ, P.PROP_NM, P.PROP_DT, P.PROP_STATUS, P.USE_START_DT, P.USE_END_DT FROM RENT_PROP P JOIN RENT_BASIC B ON ( P.RENT_SEQ = B.RENT_SEQ) JOIN USER_INFO I ON( P.USER_SEQ = I.USER_SEQ) WHERE P.USER_SEQ = ? AND P.RENT_SEQ = ? AND PROP_STATUS = '\uC644\uB8CC') WHERE RNUM BETWEEN ? AND ?
insertImage=INSERT INTO USER_GRADE_UP VALUES(USER_GRADE_UP_SEQ.NEXTVAL, ?, '\uC2E0\uCCAD', SYSDATE, DEFAULT)
insertAttachment=INSERT INTO ATTACHMENT VALUES(ATTCH_SEQ.NEXTVAL, ?, ?, ?, SYSDATE, 1 , default, default, ?, default)